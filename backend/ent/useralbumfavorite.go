// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"lark/backend/ent/album"
	"lark/backend/ent/user"
	"lark/backend/ent/useralbumfavorite"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserAlbumFavorite is the model entity for the UserAlbumFavorite schema.
type UserAlbumFavorite struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserAlbumFavoriteQuery when eager-loading is set.
	Edges                UserAlbumFavoriteEdges `json:"edges"`
	album_user_favorites *int
	user_album_favorites *int
	selectValues         sql.SelectValues
}

// UserAlbumFavoriteEdges holds the relations/edges for other nodes in the graph.
type UserAlbumFavoriteEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Album holds the value of the album edge.
	Album *Album `json:"album,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserAlbumFavoriteEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// AlbumOrErr returns the Album value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserAlbumFavoriteEdges) AlbumOrErr() (*Album, error) {
	if e.Album != nil {
		return e.Album, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: album.Label}
	}
	return nil, &NotLoadedError{edge: "album"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserAlbumFavorite) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case useralbumfavorite.FieldID:
			values[i] = new(sql.NullInt64)
		case useralbumfavorite.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case useralbumfavorite.ForeignKeys[0]: // album_user_favorites
			values[i] = new(sql.NullInt64)
		case useralbumfavorite.ForeignKeys[1]: // user_album_favorites
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserAlbumFavorite fields.
func (_m *UserAlbumFavorite) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case useralbumfavorite.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case useralbumfavorite.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case useralbumfavorite.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field album_user_favorites", value)
			} else if value.Valid {
				_m.album_user_favorites = new(int)
				*_m.album_user_favorites = int(value.Int64)
			}
		case useralbumfavorite.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_album_favorites", value)
			} else if value.Valid {
				_m.user_album_favorites = new(int)
				*_m.user_album_favorites = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserAlbumFavorite.
// This includes values selected through modifiers, order, etc.
func (_m *UserAlbumFavorite) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserAlbumFavorite entity.
func (_m *UserAlbumFavorite) QueryUser() *UserQuery {
	return NewUserAlbumFavoriteClient(_m.config).QueryUser(_m)
}

// QueryAlbum queries the "album" edge of the UserAlbumFavorite entity.
func (_m *UserAlbumFavorite) QueryAlbum() *AlbumQuery {
	return NewUserAlbumFavoriteClient(_m.config).QueryAlbum(_m)
}

// Update returns a builder for updating this UserAlbumFavorite.
// Note that you need to call UserAlbumFavorite.Unwrap() before calling this method if this UserAlbumFavorite
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserAlbumFavorite) Update() *UserAlbumFavoriteUpdateOne {
	return NewUserAlbumFavoriteClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserAlbumFavorite entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserAlbumFavorite) Unwrap() *UserAlbumFavorite {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserAlbumFavorite is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserAlbumFavorite) String() string {
	var builder strings.Builder
	builder.WriteString("UserAlbumFavorite(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserAlbumFavorites is a parsable slice of UserAlbumFavorite.
type UserAlbumFavorites []*UserAlbumFavorite
