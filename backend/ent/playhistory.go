// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"lark/backend/ent/playhistory"
	"lark/backend/ent/song"
	"lark/backend/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PlayHistory is the model entity for the PlayHistory schema.
type PlayHistory struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// PlayedAt holds the value of the "played_at" field.
	PlayedAt time.Time `json:"played_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PlayHistoryQuery when eager-loading is set.
	Edges             PlayHistoryEdges `json:"edges"`
	song_play_history *int
	user_play_history *int
	selectValues      sql.SelectValues
}

// PlayHistoryEdges holds the relations/edges for other nodes in the graph.
type PlayHistoryEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Song holds the value of the song edge.
	Song *Song `json:"song,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PlayHistoryEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// SongOrErr returns the Song value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PlayHistoryEdges) SongOrErr() (*Song, error) {
	if e.Song != nil {
		return e.Song, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: song.Label}
	}
	return nil, &NotLoadedError{edge: "song"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PlayHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case playhistory.FieldID:
			values[i] = new(sql.NullInt64)
		case playhistory.FieldPlayedAt:
			values[i] = new(sql.NullTime)
		case playhistory.ForeignKeys[0]: // song_play_history
			values[i] = new(sql.NullInt64)
		case playhistory.ForeignKeys[1]: // user_play_history
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PlayHistory fields.
func (_m *PlayHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case playhistory.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case playhistory.FieldPlayedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field played_at", values[i])
			} else if value.Valid {
				_m.PlayedAt = value.Time
			}
		case playhistory.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field song_play_history", value)
			} else if value.Valid {
				_m.song_play_history = new(int)
				*_m.song_play_history = int(value.Int64)
			}
		case playhistory.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_play_history", value)
			} else if value.Valid {
				_m.user_play_history = new(int)
				*_m.user_play_history = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PlayHistory.
// This includes values selected through modifiers, order, etc.
func (_m *PlayHistory) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the PlayHistory entity.
func (_m *PlayHistory) QueryUser() *UserQuery {
	return NewPlayHistoryClient(_m.config).QueryUser(_m)
}

// QuerySong queries the "song" edge of the PlayHistory entity.
func (_m *PlayHistory) QuerySong() *SongQuery {
	return NewPlayHistoryClient(_m.config).QuerySong(_m)
}

// Update returns a builder for updating this PlayHistory.
// Note that you need to call PlayHistory.Unwrap() before calling this method if this PlayHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PlayHistory) Update() *PlayHistoryUpdateOne {
	return NewPlayHistoryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PlayHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PlayHistory) Unwrap() *PlayHistory {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PlayHistory is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PlayHistory) String() string {
	var builder strings.Builder
	builder.WriteString("PlayHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("played_at=")
	builder.WriteString(_m.PlayedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PlayHistories is a parsable slice of PlayHistory.
type PlayHistories []*PlayHistory
