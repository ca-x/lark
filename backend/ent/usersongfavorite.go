// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"lark/backend/ent/song"
	"lark/backend/ent/user"
	"lark/backend/ent/usersongfavorite"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserSongFavorite is the model entity for the UserSongFavorite schema.
type UserSongFavorite struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserSongFavoriteQuery when eager-loading is set.
	Edges               UserSongFavoriteEdges `json:"edges"`
	song_user_favorites *int
	user_song_favorites *int
	selectValues        sql.SelectValues
}

// UserSongFavoriteEdges holds the relations/edges for other nodes in the graph.
type UserSongFavoriteEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Song holds the value of the song edge.
	Song *Song `json:"song,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserSongFavoriteEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// SongOrErr returns the Song value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserSongFavoriteEdges) SongOrErr() (*Song, error) {
	if e.Song != nil {
		return e.Song, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: song.Label}
	}
	return nil, &NotLoadedError{edge: "song"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserSongFavorite) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usersongfavorite.FieldID:
			values[i] = new(sql.NullInt64)
		case usersongfavorite.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case usersongfavorite.ForeignKeys[0]: // song_user_favorites
			values[i] = new(sql.NullInt64)
		case usersongfavorite.ForeignKeys[1]: // user_song_favorites
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserSongFavorite fields.
func (_m *UserSongFavorite) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usersongfavorite.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case usersongfavorite.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case usersongfavorite.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field song_user_favorites", value)
			} else if value.Valid {
				_m.song_user_favorites = new(int)
				*_m.song_user_favorites = int(value.Int64)
			}
		case usersongfavorite.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_song_favorites", value)
			} else if value.Valid {
				_m.user_song_favorites = new(int)
				*_m.user_song_favorites = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserSongFavorite.
// This includes values selected through modifiers, order, etc.
func (_m *UserSongFavorite) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserSongFavorite entity.
func (_m *UserSongFavorite) QueryUser() *UserQuery {
	return NewUserSongFavoriteClient(_m.config).QueryUser(_m)
}

// QuerySong queries the "song" edge of the UserSongFavorite entity.
func (_m *UserSongFavorite) QuerySong() *SongQuery {
	return NewUserSongFavoriteClient(_m.config).QuerySong(_m)
}

// Update returns a builder for updating this UserSongFavorite.
// Note that you need to call UserSongFavorite.Unwrap() before calling this method if this UserSongFavorite
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserSongFavorite) Update() *UserSongFavoriteUpdateOne {
	return NewUserSongFavoriteClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserSongFavorite entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserSongFavorite) Unwrap() *UserSongFavorite {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserSongFavorite is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserSongFavorite) String() string {
	var builder strings.Builder
	builder.WriteString("UserSongFavorite(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserSongFavorites is a parsable slice of UserSongFavorite.
type UserSongFavorites []*UserSongFavorite
